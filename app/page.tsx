/**
 * Slice 1: build 検証用のスタブ。
 * T-101 (HomeRedirector) で localStorage 判定 → /local or /ingredients に置き換える。
 */
export default function HomePage() {
  return (
    <main>
      <h1>Hello</h1>
      <p>Make Local Pizza Recipe Agent - Slice 1 scaffold</p>
    </main>
  );
}
